# Database Configuration
# Currently supports PostgreSQL only
# SQL Server support is planned (see docs/MSSQL_STATUS.md)

# PostgreSQL Connection String
DATABASE_URL=postgresql://username:password@localhost:5432/classtop

# For Windows Server users: PostgreSQL works great on Windows!
# Download: https://www.postgresql.org/download/windows/
# Or use Docker: docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=yourpassword postgres:14

# Server Configuration
HOST=0.0.0.0
PORT=8765

# Application
APP_VERSION=1.0.0

# Logging
# Options: trace, debug, info, warn, error
RUST_LOG=info

# Authentication Configuration
# IMPORTANT: Generate a strong secret key for production!
# Example: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production

# Enable/disable authentication (true/false)
# Set to false during development for easier testing
ENABLE_AUTH=true

# CORS Configuration
# Comma-separated list of allowed origins
# For development, include both frontend dev server and production URLs
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8765
